[package]
name = "harness-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AgenticBox microkernel: plugin registry + lifecycle for the agent harness"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tracing = "0.1"
shared-types = { path = "../shared-types" }
